#!/bin/sh
#|-*- mode:lisp -*-|#
#| <Put a one-line description here>
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (require :mykorz))

;(defpackage :ros.script.korz.3676667651
;  (:use :cl))
(in-package mykorz)

(defun main (&rest argv)
  (if (null argv)
      (korz-repl)
      (run (elt argv 0))))
  
;;; vim: set ft=lisp lisp:
